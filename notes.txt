- each Docker image must have the same name as its corresponding service --> mine get called srcs-service
automatically, is it normal ?

- php-fpm (it must be installed and configured) --> is it configured ?

- create admin user (in mariadb script or wordpress script ?)

- it is mandatory to use a .env file -->
modif made : .env file for DB_NAME, ADMIN_EMAIL and domain_name, added the env_file part to nginx in compose yaml and
edited scripts accordingly

- make automatic the creation of data/mariadb and data/wordpress
- how to make the folder creation in /home/apeuget/ so that it adapts to the person's login ?
    --> quickest solution = ask the person to create the folder and change manually the path in the files

TODO: finish readme, glossary in dev doc and user doc

For review : prepare folder with all files needed that can't go in git (.env, /secrets)